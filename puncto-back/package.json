{
  "devDependencies": {
    "@types/express": "^4.17.12",
    "@types/jest": "^26.0.23",
    "@types/node": "^15.6.1",
    "babel-jest": "^27.0.1",
    "jest": "^27.0.1",
    "nodemon": "^2.0.7",
    "ts-jest": "^27.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.2"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "mongodb": "docker compose up -d",
    "start": "npm run build && npm run mongodb && node out/index.js",
    "dev": "nodemon --config nodemon.json src/index.ts",
    "dev:debug": "nodemon --config nodemon.json --inspect-brk src/index.ts"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^3.6.9",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.2.32"
  }
}
